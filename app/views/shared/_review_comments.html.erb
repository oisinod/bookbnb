  <div class="card">
  <% @reviews.each do |review| %>
  <% next if review[0].review.nil? %>
  <%= " #{review[0].review.content} #{"⭐" * review[0].review.rating} by"%>
 <p>Written by:<%= link_to " #{review[0].user.user_name}", user_path(review[0].user) %></p>
  </div>
  <% end %>
