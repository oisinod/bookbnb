<div class="container">
  <div class="card">
    <%= simple_form_for(@book) do |f| %>
      <%= f.input :title %>
      <%= f.input :author %>
      <%= f.input :description %>
      <%= f.input :suggested_price, label: "Suggested price per day (€)" %>
      <%= f.input :photo, as: :file%>
      <%= f.submit 'Add book', class: 'btn btn-ghost' %>
    <% end %>
  </div>
</div>
